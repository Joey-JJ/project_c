import React from "react";
import type { ChargeStationType } from "../Types/ChargeStationType";

const ChargeStation: React.FC<{ station: ChargeStationType }> = () => {
  return <div>ChargeStation</div>;
};

export default ChargeStation;
